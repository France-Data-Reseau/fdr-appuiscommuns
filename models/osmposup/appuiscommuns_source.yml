version: 2

sources:
  - name: "appuiscommuns" # TODO _source or one file per folder / source 
    description: les données externes immportées dans appuiscommuns (par CKAN IHM ou API, ou connexion SQL ex. Airbyte ELT)
    schema: "appuiscommuns"
    #meta: {<dictionary>}
    #tags: [<string>]
    tables:
      - name: "osmgeodatamine_powersupports" # "Supports aériens OpenStreetMap" ; TODO rather a view union of all name-matching tables / views, themselves TODO _ckan
