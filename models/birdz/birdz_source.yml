version: 2

sources:
  - name: "birdz" # TODO _source or one file per folder / source
    description: les données externes immportées dans appuiscommuns (par CKAN IHM ou API, ou connexion SQL ex. Airbyte ELT)
    schema: "appuiscommuns"
    #meta: {<dictionary>}
    #tags: [<string>]
    tables:
      - name: "birdz_supportaerien" # "Supports aériens OpenStreetMap" ; TODO is a view union of all name-matching tables / views, themselves TODO _ckan
